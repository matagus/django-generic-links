============
Installation
============

At the command line::

    $ easy_install generic_links

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv generic_links
    $ pip install generic_links
